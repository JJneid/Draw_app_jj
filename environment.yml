name: dash_draw_deploy #Environment name
dependencies:
  - python=3.6
  - pip
  - pip:
    - dash
    - dash-renderer
    - dash-core-components
    - dash-html-components
    - plotly
    - gunicorn # for app deployment
    - dash-canvas
    - tensorflow-cpu
    - pandas
